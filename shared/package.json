{
  "name": "@loom/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Loom",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
